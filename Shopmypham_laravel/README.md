
### 1. Làm sao để bắt đầu?
##### Bắt đầu
```
composer install
php artisan serve
php artisan migrate
```
##### Chạy dự án trên server
```
php artisan optimize --force
composer install --prefer-dist --no-dev -o
```
##### Cache route
Để liệt kê các tuyến đường cho một địa phương nhất định, hãy sử dụng
```
php artisan route:cache


thông tin đăng nhập trang admin

domain/admin tk admin@gmail.com mk 123456


rmdir public\storage
php artisan storage:link


